<nav
  class="fixed top-0 right-0 left-0 flex justify-between items-center px-8 md:px-10 py-6 bg-surface dark:bg-surface-dark shadow-md z-50 transition-colors duration-300"
>
  <!-- Logo -->
  <a
    href="/"
    class="text-2xl md:text-3xl font-bold text-primary dark:text-primary font-primary"
  >
    🥭 Mangoland
  </a>

  <!-- Desktop Menu -->
  <ul class="hidden md:flex gap-6 text-text dark:text-text-dark font-secondary">
    <li>
      <a
        href="#hero"
        class="hover:text-primary dark:hover:text-primary transition">Home</a
      >
    </li>
    <li>
      <a
        href="#about"
        class="hover:text-primary dark:hover:text-primary transition"
        >Sobre Nosotros</a
      >
    </li>
    <li>
      <a
        href="#products"
        class="hover:text-primary dark:hover:text-primary transition"
        >Nuestros Productos</a
      >
    </li>
    <li>
      <a
        href="#why-choose-us"
        class="hover:text-primary dark:hover:text-primary transition"
        >Por Qué Elegirnos</a
      >
    </li>
    <li>
      <a
        href="#testimonials"
        class="hover:text-primary dark:hover:text-primary transition"
        >Testimonios</a
      >
    </li>
    <li>
      <a
        href="#contact"
        class="hover:text-primary dark:hover:text-primary transition"
        >Contacto</a
      >
    </li>
  </ul>

  <!-- Icons -->
  <div class="flex gap-4 items-center">
    <!-- Dark Mode Toggle -->
    <button
      id="theme-toggle"
      class="text-primary dark:text-primary text-xl"
      aria-label="Toggle Dark Mode"
    >
      🌞
    </button>

    <!-- Mobile Menu Button -->
    <button
      id="menu-button"
      class="md:hidden text-primary dark:text-primary text-2xl"
      aria-label="Abrir menú"
    >
      ☰
    </button>
  </div>

  <!-- Mobile Menu -->
  <ul
    id="mobile-menu"
    class="hidden absolute top-20 left-0 w-full h-screen bg-surface dark:bg-surface-dark shadow-lg py-6 flex-col gap-6 text-center text-text dark:text-text-dark font-secondary md:hidden transition-colors duration-300"
  >
    <li>
      <a
        href="#hero"
        class="hover:text-primary dark:hover:text-primary transition">Home</a
      >
    </li>
    <li>
      <a
        href="#about"
        class="hover:text-primary dark:hover:text-primary transition"
        >Sobre Nosotros</a
      >
    </li>
    <li>
      <a
        href="#products"
        class="hover:text-primary dark:hover:text-primary transition"
        >Nuestros Productos</a
      >
    </li>
    <li>
      <a
        href="#why-choose-us"
        class="hover:text-primary dark:hover:text-primary transition"
        >Por Qué Elegirnos</a
      >
    </li>
    <li>
      <a
        href="#testimonials"
        class="hover:text-primary dark:hover:text-primary transition"
        >Testimonios</a
      >
    </li>
    <li>
      <a
        href="#contact"
        class="hover:text-primary dark:hover:text-primary transition"
        >Contacto</a
      >
    </li>
  </ul>
</nav>

<script>
  const menuButton = document.getElementById("menu-button");
  const mobileMenu = document.getElementById("mobile-menu");
  const themeToggle = document.getElementById("theme-toggle");
  const html = document.documentElement;

  if (menuButton && mobileMenu && themeToggle && html) {
    // Mobile menu toggle
    menuButton.addEventListener("click", () => {
      if (mobileMenu.classList.contains("hidden")) {
        mobileMenu.classList.remove("hidden");
        mobileMenu.classList.add("flex");
        menuButton.textContent = "✖";
      } else {
        mobileMenu.classList.remove("flex");
        mobileMenu.classList.add("hidden");
        menuButton.textContent = "☰";
      }
    });

    // Close mobile menu when clicking any link
    const mobileLinks = mobileMenu.querySelectorAll("a");

    mobileLinks.forEach((link) => {
      link.addEventListener("click", () => {
        mobileMenu.classList.remove("flex");
        mobileMenu.classList.add("hidden");
        menuButton.textContent = "☰";
      });
    });

    // Theme toggle (Light/Dark)
    const savedTheme = localStorage.getItem("theme");
    if (savedTheme) {
      html.className = savedTheme;
      themeToggle.textContent = savedTheme === "dark" ? "🌙" : "🌞";
    }

    themeToggle.addEventListener("click", () => {
      html.classList.toggle("dark");
      html.classList.toggle("light");

      const newTheme = html.classList.contains("dark") ? "dark" : "light";
      localStorage.setItem("theme", newTheme);
      themeToggle.textContent = newTheme === "dark" ? "🌙" : "🌞";
    });
  }
</script>
